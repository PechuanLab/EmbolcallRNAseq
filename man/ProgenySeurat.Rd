% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProgenySeurat.R
\name{ProgenySeurat}
\alias{ProgenySeurat}
\title{Compute and plot PROGENy pathway activities for Seurat clusters}
\usage{
ProgenySeurat(
  seu,
  id_col = NULL,
  organism = c("Human", "Mouse"),
  top = 100,
  perm = 1,
  return_assay = TRUE,
  assay_name = "progeny",
  scale_assay = TRUE,
  outfile = "Progeny_pathway_by_detailedID.pdf",
  width = 12,
  top_bar = NULL
)
}
\arguments{
\item{seu}{A Seurat object.}

\item{id_col}{Optional character. Column name in Seurat metadata to use as cell
identities (if provided, \code{Idents(seu)} will be set to this column).}

\item{organism}{Character. "Human" or "Mouse" (passed to PROGENy). Default "Human".}

\item{top}{Integer. Number of top target genes per pathway for PROGENy. Default 100.}

\item{perm}{Integer. Number of permutations for PROGENy. Default 1.}

\item{return_assay}{Logical. If TRUE, PROGENy results are returned as a new assay.
Default TRUE.}

\item{assay_name}{Character. Name to use for the PROGENy assay. Default "progeny".}

\item{scale_assay}{Logical. If TRUE, scales the PROGENy assay using Seurat::ScaleData. Default TRUE.}

\item{outfile}{Character. Path to output PDF heatmap file. Default "Progeny_pathway_by_detailedID.pdf".}

\item{width}{Numeric. Width (in inches) of the output PDF. Default 12.}

\item{top_bar}{Optional. Top annotation object for ComplexHeatmap::Heatmap (passed through). Default NULL.}
}
\value{
A list with elements \code{seu} (the Seurat object with added assay) and
  \code{summary} (data.frame with average and sd pathway activities per cluster).
}
\description{
Runs PROGENy on a Seurat object, adds the pathway activity assay, scales it,
summarizes activities by cluster (or provided identity column), and saves a
heatmap of averaged pathway activities per cluster.
}
\examples{
\dontrun{
  out <- ProgenySeurat(seu, id_col = "ClusterFine", organism = "Human",
                      top = 100, outfile = "Progeny.pdf")
  head(out$summary)
}
}
