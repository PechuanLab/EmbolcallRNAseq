% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ThetaProjectionScore.R
\name{ThetaProjectionScore}
\alias{ThetaProjectionScore}
\title{Sweep theta values for projection score analysis}
\usage{
ThetaProjectionScore(
  DataMatrix,
  NPCs = 3,
  nboot = 100,
  thetas = c(0.25, 0.5, 0.75, 1)
)
}
\arguments{
\item{DataMatrix}{Matrix. Numeric data matrix (features x samples) to analyze.}

\item{NPCs}{Integer. Number of principal components to use. Default is 3.}

\item{nboot}{Integer. Number of bootstrap iterations. Default is 100.}

\item{thetas}{Numeric vector. Fraction(s) of most variable features to use (between 0 and 1). Default is c(0.25, 0.5, 0.75, 1).}
}
\value{
Data frame. Columns: Thetas (fraction of features used), ProjectionScores (score for each theta).
}
\description{
Calculates projection scores for a range of theta values, where theta determines the fraction of most variable features to use. For each theta, the function selects the top variable features, computes the projection score, and returns a data frame of results. Useful for evaluating the effect of feature selection on projection stability.
}
\examples{
# Example with random data
set.seed(123)
mat <- matrix(rnorm(1000), nrow = 100, ncol = 10)
ps.df <- ThetaProjectionScore(mat, NPCs = 3, nboot = 50, thetas = c(0.1, 0.25, 0.5, 1))
}
