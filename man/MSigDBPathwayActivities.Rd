% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MSigDBPathwayActivities.R
\name{MSigDBPathwayActivities}
\alias{MSigDBPathwayActivities}
\title{Compute MSigDB pathway activities for a Seurat object}
\usage{
MSigDBPathwayActivities(
  seu,
  species = "auto",
  collection = "H",
  subcollection = NULL,
  gene_sets = NULL,
  method = c("seurat", "ucell"),
  assay = NULL,
  layer = NULL,
  slot = NULL,
  assay_name = "Pathway_Activities",
  min_genes = 5,
  overwrite = TRUE,
  nbin = 24,
  ctrl = 100,
  seed = 1,
  search = FALSE,
  maxRank = 1500,
  ncores = 1,
  BPPARAM = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{seu}{Seurat object.}

\item{species}{Character. One of \code{"auto"}, \code{"human"},
\code{"mouse"}, \code{"Homo sapiens"}, or \code{"Mus musculus"}. When
\code{"auto"}, the species is inferred from overlap between assay features
and human/mouse MSigDB genes. Default \code{"auto"}.}

\item{collection}{Character vector. MSigDB collection(s) to use. Default
\code{"H"} for Hallmark.}

\item{subcollection}{Optional character vector of MSigDB subcollection(s) to
use.}

\item{gene_sets}{Optional named list of gene symbols. When supplied, these
gene sets are used instead of fetching MSigDB.}

\item{method}{Character. Scoring backend: \code{"seurat"} for
\code{Seurat::AddModuleScore} or \code{"ucell"} for UCell rank-based
scores. Aliases \code{"module"}, \code{"addmodulescore"}, \code{"u"}, and
\code{"u_module"} are accepted.}

\item{assay}{Character. Assay to score. Defaults to the active assay.}

\item{layer}{Optional Seurat v5 layer to use when extracting data for
filtering and UCell scoring. For \code{method = "seurat"}, this value is
passed to \code{Seurat::AddModuleScore} as its \code{slot} argument.}

\item{slot}{Character. Seurat v4 slot/layer fallback. Defaults to
\code{"data"} for Seurat module scores and \code{"counts"} for UCell.}

\item{assay_name}{Character. Name of the assay to add. Default
\code{"Pathway_Activities"}.}

\item{min_genes}{Integer. Minimum number of genes from a pathway that must be
present in the selected assay. Default \code{5}.}

\item{overwrite}{Logical. If \code{TRUE}, replace an existing assay named
\code{assay_name}. Default \code{TRUE}.}

\item{nbin}{Integer. Number of expression bins passed to
\code{Seurat::AddModuleScore}. Default \code{24}.}

\item{ctrl}{Integer. Number of control genes passed to
\code{Seurat::AddModuleScore}. Default \code{100}.}

\item{seed}{Integer. Random seed passed to \code{Seurat::AddModuleScore}.
Default \code{1}.}

\item{search}{Logical. Passed to \code{Seurat::AddModuleScore}. Default
\code{FALSE}.}

\item{maxRank}{Integer. Maximum rank passed to
\code{UCell::ScoreSignatures_UCell}. Default \code{1500}.}

\item{ncores}{Integer. Number of cores passed to UCell. Default \code{1}.}

\item{BPPARAM}{Optional BiocParallel parameter object for UCell. If
\code{NULL}, \code{BiocParallel::SerialParam()} is used.}

\item{verbose}{Logical. If \code{TRUE}, report selected species and retained
pathway counts. Default \code{TRUE}.}

\item{...}{Additional arguments passed to the selected scoring backend.}
}
\value{
The input Seurat object with a new assay containing pathway
  activities in the \code{data} layer/slot. Original MSigDB pathway names and
  retained gene counts are stored in the pathway assay feature metadata.
}
\description{
Scores MSigDB gene sets in a Seurat object and stores the resulting
pathway-by-cell matrix as a new assay. By default, Hallmark pathways are
loaded from MSigDB for the inferred organism.
}
\examples{
\dontrun{
seu <- MSigDBPathwayActivities(seu)
seu <- MSigDBPathwayActivities(seu, species = "mouse", method = "ucell")
seu <- MSigDBPathwayActivities(seu, collection = "C2", subcollection = "CP:REACTOME")
}
}
