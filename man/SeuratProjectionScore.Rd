% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeuratProjectionScore.R
\name{SeuratProjectionScore}
\alias{SeuratProjectionScore}
\title{Calculate projection score from a Seurat object}
\usage{
SeuratProjectionScore(
  seu,
  NPCs = 3,
  nboot = 100,
  assay = NULL,
  layer = NULL,
  slot = "data",
  features = NULL,
  use.variable.features = TRUE,
  strict.features = FALSE
)
}
\arguments{
\item{seu}{Seurat object.}

\item{NPCs}{Integer. Number of principal components to use. Default is 3.}

\item{nboot}{Integer. Number of bootstrap iterations. Default is 100.}

\item{assay}{Character. Assay to use. Defaults to the object's active assay.}

\item{layer}{Character. Seurat v5 layer to extract. If \code{NULL}, the
function tries \code{slot} as a layer first and then as a Seurat v4 slot.}

\item{slot}{Character. Seurat v4 slot fallback. Default is \code{"data"}.}

\item{features}{Character vector of features to use. If supplied, this takes
precedence over \code{use.variable.features}.}

\item{use.variable.features}{Logical. Use \code{VariableFeatures(seu)} when
\code{features} is not supplied and variable features are available.
Default is \code{TRUE}.}

\item{strict.features}{Logical. If \code{TRUE}, error when any requested
feature is absent from the assay matrix. If \code{FALSE}, absent features
are dropped with a warning. Default is \code{FALSE}.}
}
\value{
Numeric projection score.
}
\description{
Extracts an assay matrix from a Seurat object and passes it to
\code{ProjectionScore}. By default, the function uses the active assay and the
normalized \code{data} layer/slot. If variable features are present, they are
used by default to keep the matrix focused on informative genes.
}
\examples{
\dontrun{
score <- SeuratProjectionScore(seu, assay = "RNA", layer = "data")
score_all_features <- SeuratProjectionScore(seu, use.variable.features = FALSE)
}
}
